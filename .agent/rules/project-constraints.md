---
trigger: always_on
---

- every db query source should be to cloud first, if cloud is not available, then use local db
- for typechecking just  run `npx tsc --noEmit`
- sebisa mungkin component ui dibuat terpisah dan modular
